# 🚀 Развертывание автопостинг-бота на VPS с существующим ботом

## ✅ Гарантия отсутствия конфликтов

**Этот бот НЕ будет конфликтовать с вашим существующим ботом**, потому что:

1. ✅ **Не использует порты** - только исходящие соединения к Telegram API
2. ✅ **Изолированные процессы** - каждый бот работает в отдельном процессе
3. ✅ **Разные файлы конфигурации** - отдельные `.env` и `session.txt`
4. ✅ **Разные директории** - полная изоляция проектов

---

## 📋 Пошаговая инструкция

### Шаг 1: Подключение к VPS

```bash
ssh user@your-vps-ip
```

### Шаг 2: Проверка существующего бота

Проверьте, как запущен ваш текущий бот:

```bash
# Если используется PM2
pm2 list

# Если используется systemd
systemctl list-units --type=service | grep bot

# Если используется Docker
docker ps
```

**Важно:** Запомните имя вашего текущего бота (например, `my-bot`, `telegram-bot` и т.д.)

### Шаг 3: Клонирование проекта в отдельную директорию

```bash
# Создайте отдельную директорию для нового бота
cd /opt  # или /home/your-user или любая другая директория
git clone https://github.com/Invidus/autopost.git autoposting-bot
cd autoposting-bot
```

**Важно:** Используйте **другую директорию**, чем у вашего существующего бота!

Пример структуры:
```
/opt/
├── your-existing-bot/     # Ваш текущий бот
└── autoposting-bot/       # Новый автопостинг бот
```

### Шаг 4: Установка зависимостей

```bash
# Проверьте версию Node.js (должна быть 18+)
node -v

# Если Node.js не установлен или версия < 18
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# Установка зависимостей проекта
npm install
```

### Шаг 5: Настройка конфигурации

```bash
# Создайте .env файл из примера
cp env.example .env

# Откройте для редактирования
nano .env
```

Заполните следующие параметры:

```env
# Получите на https://my.telegram.org
API_ID=your_api_id
API_HASH=your_api_hash

# ID целевого канала (куда будут пересылаться сообщения)
# Формат: @channel_username или -1001234567890
TARGET_CHANNEL_ID=@your_target_channel

# Каналы-источники (через запятую)
# Формат: @channel1,@channel2 или -1001234567890,-1009876543210
SOURCE_CHANNELS=@source_channel1,@source_channel2

# Задержка между постами (в секундах)
POST_DELAY=60

# Фильтр ключевых слов (опционально, через запятую)
KEYWORDS_FILTER=

# Исключающие слова (опционально, через запятую)
EXCLUDE_WORDS=
```

**Сохраните файл:** `Ctrl+O`, затем `Enter`, затем `Ctrl+X`

### Шаг 6: Первая авторизация

```bash
# Запустите бота для первой авторизации
npm start
```

Вам нужно будет ввести:
1. Номер телефона (с кодом страны, например: +79991234567)
2. Код подтверждения из Telegram
3. Пароль (если установлен двухфакторная аутентификация)

После успешной авторизации:
- Файл `session.txt` будет создан автоматически
- Нажмите `Ctrl+C` для остановки

### Шаг 7: Запуск через PM2 (Рекомендуется)

PM2 позволяет управлять несколькими ботами одновременно без конфликтов.

```bash
# Запуск с уникальным именем
pm2 start ecosystem.config.js

# Или вручную с явным указанием имени
pm2 start index.js --name autoposting-bot

# Сохранение конфигурации PM2
pm2 save

# Настройка автозапуска при перезагрузке (если еще не настроено)
pm2 startup
# Выполните команду, которую выведет PM2
```

**Проверка работы:**

```bash
# Список всех ботов
pm2 list

# Вы должны увидеть оба бота:
# ┌─────┬──────────────────┬─────────┬─────────┬──────────┐
# │ id  │ name             │ status  │ restart │ uptime   │
# ├─────┼──────────────────┼─────────┼─────────┼──────────┤
# │ 0   │ your-existing-bot│ online  │ 0       │ 2h       │
# │ 1   │ autoposting-bot  │ online  │ 0       │ 1m       │
# └─────┴──────────────────┴─────────┴─────────┴──────────┘

# Логи нового бота
pm2 logs autoposting-bot

# Статус
pm2 status autoposting-bot
```

### Шаг 8: Альтернатива - Systemd (если не используете PM2)

Если ваш существующий бот использует systemd, вы можете использовать его и для нового:

```bash
# Отредактируйте service файл
nano autoposting-bot.service

# Измените:
# - User=your-username (замените на ваше имя пользователя)
# - WorkingDirectory=/opt/autoposting-bot (путь к проекту)
# - ExecStart=/usr/bin/node /opt/autoposting-bot/index.js (путь к node)

# Скопируйте service файл
sudo cp autoposting-bot.service /etc/systemd/system/

# Перезагрузите systemd
sudo systemctl daemon-reload

# Включите автозапуск
sudo systemctl enable autoposting-bot

# Запустите сервис
sudo systemctl start autoposting-bot

# Проверка статуса
sudo systemctl status autoposting-bot

# Просмотр логов
sudo journalctl -u autoposting-bot -f
```

### Шаг 9: Альтернатива - Docker (если используете Docker)

Если ваш существующий бот в Docker:

```bash
# Соберите образ
docker build -t autoposting-bot .

# Запустите контейнер с уникальным именем
docker run -d \
  --name autoposting-bot \
  --restart unless-stopped \
  -v $(pwd)/.env:/app/.env:ro \
  -v $(pwd)/session.txt:/app/session.txt \
  autoposting-bot

# Или используйте docker-compose
docker-compose up -d

# Проверка
docker ps
# Вы должны увидеть оба контейнера
```

---

## 🔍 Проверка отсутствия конфликтов

### 1. Проверка процессов

```bash
# Все процессы Node.js
ps aux | grep node

# PM2 процессы
pm2 list

# Docker контейнеры
docker ps
```

### 2. Проверка файлов

```bash
# Убедитесь, что файлы в разных директориях
ls -la /opt/your-existing-bot/.env
ls -la /opt/autoposting-bot/.env

# Убедитесь, что session.txt разные
ls -la /opt/your-existing-bot/session.txt
ls -la /opt/autoposting-bot/session.txt
```

### 3. Проверка работы обоих ботов

```bash
# Логи существующего бота
pm2 logs your-existing-bot

# Логи нового бота
pm2 logs autoposting-bot

# Оба должны работать без ошибок
```

---

## 📊 Управление автопостинг-ботом

### PM2 команды:

```bash
pm2 start autoposting-bot      # Запуск
pm2 stop autoposting-bot       # Остановка
pm2 restart autoposting-bot    # Перезапуск
pm2 delete autoposting-bot     # Удаление из PM2
pm2 logs autoposting-bot       # Просмотр логов
pm2 logs autoposting-bot --lines 100  # Последние 100 строк
pm2 monit                      # Мониторинг всех процессов
```

### Systemd команды:

```bash
sudo systemctl start autoposting-bot
sudo systemctl stop autoposting-bot
sudo systemctl restart autoposting-bot
sudo systemctl status autoposting-bot
sudo journalctl -u autoposting-bot -f
```

### Docker команды:

```bash
docker start autoposting-bot
docker stop autoposting-bot
docker restart autoposting-bot
docker logs autoposting-bot
docker logs autoposting-bot -f  # Следить за логами
```

---

## 🔄 Обновление бота

```bash
cd /opt/autoposting-bot
git pull
npm install

# Перезапуск
pm2 restart autoposting-bot
# или
sudo systemctl restart autoposting-bot
# или
docker restart autoposting-bot
```

### 📋 Проверка на дубликаты

Бот автоматически проверяет дубликаты сообщений:
- ✅ Создает файл `posted_hashes.json` для хранения хешей
- ✅ При старте проверяет последние 100 сообщений в канале
- ✅ Не отправляет одинаковые сообщения повторно

**Файл создается автоматически** - никаких дополнительных действий не требуется!

Подробная инструкция по обновлению: см. [UPDATE_ON_VPS.md](./UPDATE_ON_VPS.md)

---

## 🛠️ Устранение проблем

### Бот не запускается

1. **Ошибка `ERR_UNSUPPORTED_DIR_IMPORT` при импорте `telegram/sessions`:**

   Эта ошибка возникает в Node.js 20+ при использовании ES модулей. **Исправление уже включено в код**, но если вы клонировали старую версию:

   ```bash
   # Обновите код из репозитория
   git pull

   # Или вручную исправьте в index.js строку 2:
   # Было: import { StringSession } from "telegram/sessions";
   # Стало: import { StringSession } from "telegram/sessions/index.js";
   ```

2. **Ошибка PM2 "No script path - aborting":**

   Убедитесь, что вы запускаете PM2 из директории проекта:
   ```bash
   cd /opt/autoposting-bot
   pm2 start ecosystem.config.js
   # или
   pm2 start index.js --name autoposting-bot
   ```

3. **Проверьте логи:**
   ```bash
   pm2 logs autoposting-bot --err
   # или
   sudo journalctl -u autoposting-bot -n 50
   ```

4. **Проверьте .env файл:**
   ```bash
   cat .env
   # Убедитесь, что все поля заполнены
   ```

5. **Проверьте session.txt:**
   ```bash
   ls -la session.txt
   # Файл должен существовать после первой авторизации
   ```

### Конфликт с существующим ботом

Если возникли проблемы:

1. **Проверьте имена в PM2:**
   ```bash
   pm2 list
   # Убедитесь, что имена разные
   ```

2. **Проверьте директории:**
   ```bash
   pwd
   # Должна быть /opt/autoposting-bot (или другая, но не та же, что у существующего бота)
   ```

3. **Проверьте порты (хотя этот бот их не использует):**
   ```bash
   netstat -tulpn | grep node
   # Этот бот не должен слушать порты
   ```

### Бот падает

1. **Проверьте доступность Telegram API:**
   ```bash
   ping api.telegram.org
   ```

2. **Проверьте валидность session.txt:**
   - Удалите `session.txt` и запустите `npm start` для повторной авторизации

3. **Проверьте права доступа:**
   ```bash
   chmod 600 .env session.txt
   ```

---

## 🔒 Безопасность

1. **Ограничьте доступ к конфигурационным файлам:**
   ```bash
   chmod 600 .env session.txt
   ```

2. **Не коммитьте .env и session.txt в git:**
   - Эти файлы уже в `.gitignore`

3. **Регулярные обновления:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```

---

## ✅ Итоговая проверка

После развертывания убедитесь:

- [ ] Оба бота работают одновременно
- [ ] Разные директории для каждого бота
- [ ] Разные имена в PM2/systemd/Docker
- [ ] Разные .env и session.txt файлы
- [ ] Оба бота отвечают в Telegram
- [ ] Нет ошибок в логах

---

## 📞 Дополнительная помощь

Если возникли проблемы:

1. Проверьте логи обоих ботов
2. Убедитесь, что используются разные директории и имена
3. Проверьте документацию в `README.md` и `DEPLOYMENT.md`

**Помните:** Этот бот использует Telegram Client API (как клиент), а не Bot API, поэтому он не конфликтует с ботами, использующими Bot API.

