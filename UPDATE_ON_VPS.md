# 🔄 Обновление бота на VPS с новой функциональностью

## 📋 Что добавлено в новой версии:

1. ✅ Проверка на дубликаты по содержимому
2. ✅ Сохранение хешей в файл `posted_hashes.json`
3. ✅ Автоматическая проверка существующих сообщений в канале при старте

## 🚀 Инструкция по обновлению на VPS

### Шаг 1: Подключитесь к VPS

```bash
ssh user@your-vps-ip
```

### Шаг 2: Перейдите в директорию бота

```bash
cd /opt/autoposting-bot
```

### Шаг 3: Обновите код из репозитория

```bash
# Сохраните текущие изменения (если есть)
git stash

# Получите последние изменения
git pull origin master

# Если были локальные изменения, восстановите их
git stash pop
```

### Шаг 4: Установите зависимости (если нужно)

```bash
npm install
```

### Шаг 5: Проверьте файл .env

Убедитесь, что все настройки на месте:

```bash
nano .env
```

Проверьте наличие всех переменных:
- `API_ID`
- `API_HASH`
- `TARGET_CHANNEL_ID`
- `SOURCE_CHANNELS`
- `FOOTER_TEXT` (опционально)
- `FOOTER_LINK_TEXT` (опционально)
- `FOOTER_LINK_URL` (опционально)

### Шаг 6: Перезапустите бота

#### Если используете PM2:

```bash
# Проверьте, запущен ли бот
pm2 list

# Если бот НЕ запущен, сначала запустите его:
pm2 start ecosystem.config.cjs
pm2 save

# Если бот УЖЕ запущен, перезапустите:
pm2 restart autoposting-bot
```

**⚠️ Если видите ошибку "Process not found"** - см. [FIX_PM2_START.md](./FIX_PM2_START.md)

#### Если используете systemd:

```bash
sudo systemctl restart autoposting-bot
```

#### Если используете Docker:

```bash
docker restart autoposting-bot
```

### Шаг 7: Проверьте логи

```bash
# PM2
pm2 logs autoposting-bot --lines 50

# systemd
sudo journalctl -u autoposting-bot -n 50 -f

# Docker
docker logs autoposting-bot -f
```

Вы должны увидеть сообщения:
- `📋 Загружено X хешей отправленных сообщений` (если файл уже существует)
- `🔍 Проверяю существующие сообщения в целевом канале...`
- `✅ Найдено X существующих сообщений в канале` или `✅ Дубликатов в канале не найдено`

## 📁 Файлы, которые будут созданы автоматически:

- `posted_hashes.json` - файл с хешами отправленных сообщений
  - Создается автоматически при первом запуске
  - Сохраняется в той же директории, что и `index.js`
  - Уже добавлен в `.gitignore`, не будет коммититься в git

## 🔍 Проверка работы

### 1. Проверьте, что файл создался:

```bash
ls -la posted_hashes.json
```

### 2. Проверьте содержимое файла (первые несколько строк):

```bash
head -n 20 posted_hashes.json
```

Должен быть JSON массив с хешами.

### 3. Проверьте логи на наличие дубликатов:

Когда бот попытается отправить дубликат, в логах появится:
```
⏭️  Пропущено (дубликат по содержимому): текст сообщения...
```

## 🛠️ Управление файлом хешей

### Просмотр количества сохраненных хешей:

```bash
cat posted_hashes.json | jq '. | length'
```

Или без jq:
```bash
grep -o '"[a-f0-9]\{32\}"' posted_hashes.json | wc -l
```

### Очистка файла хешей (если нужно начать заново):

```bash
# Остановите бота
pm2 stop autoposting-bot

# Удалите файл
rm posted_hashes.json

# Запустите бота снова
pm2 start autoposting-bot
```

⚠️ **Внимание:** После удаления файла бот снова проверит последние 100 сообщений в канале при старте.

### Резервное копирование файла хешей:

```bash
# Создайте резервную копию
cp posted_hashes.json posted_hashes.json.backup

# Восстановление из резервной копии
cp posted_hashes.json.backup posted_hashes.json
```

## 🔒 Права доступа к файлу

Файл `posted_hashes.json` создается автоматически с правами по умолчанию. Если нужно ограничить доступ:

```bash
chmod 600 posted_hashes.json
```

## 📊 Мониторинг размера файла

Файл ограничен 10000 хешей (примерно 400-500 KB). Проверить размер:

```bash
ls -lh posted_hashes.json
```

## ⚠️ Возможные проблемы

### Проблема: Файл не создается

**Решение:**
1. Проверьте права на запись в директорию:
   ```bash
   ls -la /opt/autoposting-bot
   ```
2. Убедитесь, что бот запущен от правильного пользователя
3. Проверьте логи на ошибки

### Проблема: Дубликаты все равно отправляются

**Решение:**
1. Проверьте, что файл `posted_hashes.json` существует и не пустой
2. Проверьте логи на ошибки при создании хешей
3. Убедитесь, что бот имеет доступ к чтению сообщений в целевом канале

### Проблема: Файл слишком большой

**Решение:**
Файл автоматически ограничивается 10000 хешами. Если нужно уменьшить:
- Удалите файл (бот создаст новый при следующем запуске)
- Или вручную отредактируйте файл, оставив последние N хешей

## ✅ Проверка успешного обновления

После обновления проверьте:

1. ✅ Бот запустился без ошибок
2. ✅ В логах есть сообщение о загрузке/проверке хешей
3. ✅ Файл `posted_hashes.json` создан
4. ✅ Бот не отправляет дубликаты

## 🔄 Автоматическое обновление (опционально)

Можно создать скрипт для автоматического обновления:

```bash
#!/bin/bash
# update-bot.sh

cd /opt/autoposting-bot
git pull origin master
npm install
pm2 restart autoposting-bot
echo "✅ Бот обновлен!"
```

Сделайте скрипт исполняемым:
```bash
chmod +x update-bot.sh
```

Запуск:
```bash
./update-bot.sh
```

---

## 📞 Дополнительная помощь

Если возникли проблемы:
1. Проверьте логи бота
2. Убедитесь, что все файлы обновлены
3. Проверьте права доступа к файлам
4. См. основную документацию в `DEPLOYMENT_WITH_EXISTING_BOT.md`

