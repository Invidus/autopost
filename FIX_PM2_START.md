# 🔧 Исправление ошибки PM2: Process not found

## ❌ Проблема:
```
[PM2] [ERROR] Process or Namespace autoposting-bot not found
```

Это означает, что бот еще не запущен через PM2.

## ✅ Решение:

### Вариант 1: Запуск через конфигурационный файл (Рекомендуется)

**⚠️ Сначала обновите код из репозитория:**

```bash
cd /opt/autoposting-bot
git pull
```

Затем запустите:

```bash
# Запуск бота
pm2 start ecosystem.config.cjs

# Сохранение конфигурации PM2
pm2 save

# Настройка автозапуска при перезагрузке (если еще не настроено)
pm2 startup
# Выполните команду, которую выведет PM2
```

**Если все еще ошибка "No script path":**
- Убедитесь, что файл `ecosystem.config.cjs` существует (расширение .cjs необходимо для CommonJS в проектах с ES modules)
- Или используйте Вариант 2 (прямой запуск)

### Вариант 2: Запуск напрямую

```bash
cd /opt/autoposting-bot

# Запуск с указанием имени
pm2 start index.js --name autoposting-bot

# Сохранение
pm2 save
```

## 🔍 Проверка статуса

После запуска проверьте:

```bash
# Список всех процессов PM2
pm2 list

# Должен показать:
# ┌─────┬──────────────────┬─────────┬─────────┬──────────┐
# │ id  │ name             │ status  │ restart │ uptime   │
# ├─────┼──────────────────┼─────────┼─────────┼──────────┤
# │ 0   │ autoposting-bot  │ online  │ 0       │ 1m       │
# └─────┴──────────────────┴─────────┴─────────┴──────────┘
```

## 📊 Просмотр логов

```bash
# Все логи
pm2 logs autoposting-bot

# Последние 50 строк
pm2 logs autoposting-bot --lines 50

# Следить за логами в реальном времени
pm2 logs autoposting-bot --lines 0
```

## 🔄 После первого запуска

Теперь вы можете использовать команды:

```bash
pm2 restart autoposting-bot  # Перезапуск
pm2 stop autoposting-bot     # Остановка
pm2 start autoposting-bot    # Запуск (если остановлен)
pm2 delete autoposting-bot   # Удаление из PM2
```

## ⚠️ Важные моменты

1. **Перед первым запуском убедитесь:**
   - ✅ Файл `.env` настроен
   - ✅ Выполнена первая авторизация (создан `session.txt`)
   - ✅ Установлены зависимости: `npm install`

2. **Если бот уже запущен под другим именем:**
   ```bash
   # Проверьте все процессы
   pm2 list

   # Если видите другой процесс, используйте его имя
   pm2 restart <имя_процесса>
   ```

3. **Если нужно переименовать процесс:**
   ```bash
   # Удалите старый
   pm2 delete <старое_имя>

   # Запустите с новым именем
   pm2 start index.js --name autoposting-bot
   pm2 save
   ```

## 🆘 Если бот не запускается

1. **Проверьте логи:**
   ```bash
   pm2 logs autoposting-bot --err
   ```

2. **Проверьте .env файл:**
   ```bash
   cat .env
   ```

3. **Проверьте session.txt:**
   ```bash
   ls -la session.txt
   ```

4. **Попробуйте запустить вручную для проверки:**
   ```bash
   npm start
   # Если работает, остановите (Ctrl+C) и запустите через PM2
   ```

## ✅ После успешного запуска

Вы должны увидеть в логах:
- `🚀 Запуск бота автопостинга...`
- `📋 Загружено X хешей отправленных сообщений` (если файл существует)
- `🔍 Проверяю существующие сообщения в целевом канале...`
- `✅ Авторизован как: ...`
- `🎯 Бот запущен и работает...`

